# @recoverlution/ui

Design system React components for RecoveryOS.

## Scripts

- `pnpm build` — build the component library
- `pnpm lint` — lint source files
- `pnpm test` — run unit tests

## Usage

Import components from this package in your Next.js or React app.
