{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Design tokens schema (nested)",
  "type": "object",
  "properties": {
    "primitive": { "$ref": "#/definitions/nested" },
    "semantic": { "$ref": "#/definitions/nested" },
    "component": { "$ref": "#/definitions/nested" },
    "motion": { "$ref": "#/definitions/nested" },
    "axis": { "$ref": "#/definitions/nested" },
    "brush": { "$ref": "#/definitions/nested" }
  },
  "required": ["primitive", "semantic", "motion"],
  "additionalProperties": false,
  "definitions": {
    "nested": {
      "type": "object",
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          { "type": "number" },
          { "$ref": "#/definitions/nested" }
        ]
      }
    }
  }
}
