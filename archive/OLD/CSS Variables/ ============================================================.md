```
/* ============================================================
   RecoveryOS CSS Variables — v1
   - Anchors preserved: Purple (900/700/500) + Cyan (500)
   - Adds: Growth Green + Violet-tinted neutrals
   - No reds/oranges/yellows
   - Strategy: neutral surfaces; color appears as rails/focus/seal
   ============================================================ */

/* ---------- ROOT (Light) ---------- */
:root {
  /* --- Foundation: Color --- */
  --c-purple-950: #1B1548;
  --c-purple-900: #3E2BB8; /* Primary */
  --c-purple-700: #5739FB;
  --c-purple-500: #7C67FF;
  --c-purple-200: #D6D0FF;
  --c-purple-100: #EFEAFF;

  --c-cyan-800: #0C8F86;
  --c-cyan-500: #40E0D0; /* Accent */
  --c-cyan-200: #BFF6F0;
  --c-cyan-100: #E7FFFC;

  --c-green-800: #0F7D67;
  --c-green-500: #2FE6A6; /* Growth */
  --c-green-200: #BFF8E6;
  --c-green-100: #E9FFF7;

  --c-neutral-950: #0B0A14; /* Ink */
  --c-neutral-900: #15142A;
  --c-neutral-800: #1F1E36;
  --c-neutral-700: #2A2942;
  --c-neutral-200: #E9E7FF; /* Mist border */
  --c-neutral-100: #FBFAFF; /* Bone */

  /* --- Foundation: Typography --- */
  --font-heading: "Plus Jakarta Sans", ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
  --font-body: "Inter", ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
  --font-mono: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;

  --tracking-heading-tight: -0.03em;
  --tracking-eyebrow-wide: 0.12em;

  --lh-body: 1.65;
  --lh-heading: 1.15;

  /* --- Foundation: Radius --- */
  --r-xs: 8px;
  --r-sm: 12px;
  --r-md: 16px;
  --r-lg: 24px;
  --r-xl: 32px;

  /* --- Foundation: Space --- */
  --s-1: 4px;
  --s-2: 8px;
  --s-3: 12px;
  --s-4: 16px;
  --s-5: 24px;
  --s-6: 32px;
  --s-7: 48px;
  --s-8: 64px;

  /* --- Foundation: Elevation (use with box-shadow) --- */
  --e0: 0 0 0 0 rgba(0,0,0,0);
  --e1: 0 8px 24px -8px rgba(11,10,20,0.12);
  --e2: 0 16px 48px -16px rgba(11,10,20,0.16);

  /* --- Foundation: Motion --- */
  --dur-arrive: 420ms;
  --dur-glide: 520ms;
  --dur-settle: 680ms;
  --dur-confirm: 260ms;
  --dur-carry: 560ms;

  --ease-regulate: cubic-bezier(0.2, 0.8, 0.2, 1);
  --ease-decide: cubic-bezier(0.25, 1, 0.5, 1);

  /* ============================================================
     Semantic: Global Surfaces + Ink
     ============================================================ */
  --surface-bg: var(--c-neutral-100);
  --surface-panel: var(--c-neutral-100);
  --surface-card: var(--c-neutral-100);
  --surface-elevated: var(--c-neutral-100);
  --surface-border: var(--c-neutral-200);
  --surface-divider: rgba(233,231,255,0.55);

  --ink-primary: var(--c-neutral-950);
  --ink-secondary: rgba(11,10,20,0.72);
  --ink-tertiary: rgba(11,10,20,0.52);
  --ink-inverse: var(--c-neutral-100);

  /* Focus */
  --focus-ring: var(--c-cyan-500);
  --focus-ring-subtle: rgba(64,224,208,0.35);

  /* ============================================================
     Semantic: OS Objects
     ============================================================ */
  /* Return */
  --return-surface: var(--c-purple-900);
  --return-surface-hover: var(--c-purple-700);
  --return-ink: var(--c-neutral-100);
  --return-glow: rgba(124,103,255,0.28);
  --return-focus: var(--focus-ring);

  /* Grip */
  --grip-accent: var(--c-cyan-500);
  --grip-accent-soft: var(--c-cyan-200);
  --grip-focus: var(--c-cyan-500);

  /* Trace */
  --trace-surface: var(--c-neutral-100);
  --trace-border: var(--c-neutral-200);
  --trace-ink: var(--ink-primary);
  --trace-seal: var(--c-cyan-500);
  --trace-seal-soft: var(--c-cyan-200);

  /* Thread */
  --thread-line: rgba(62,43,184,0.26);
  --thread-node: var(--c-purple-500);

  /* Thread gradient stops (for CSS gradients) */
  --thread-g0: rgba(62,43,184,0.34);
  --thread-g1: rgba(64,224,208,0.32);
  --thread-g2: rgba(47,230,166,0.32);

  /* Handrail */
  --handrail-surface: var(--c-neutral-100);
  --handrail-border: var(--c-neutral-200);
  --handrail-edge: var(--c-cyan-500);
  --handrail-ink: var(--ink-primary);

  /* Compass */
  --compass-accent: var(--c-green-500);
  --compass-accent-deep: var(--c-green-800);

  /* Drift (no warm danger colors) */
  --drift-surface: var(--c-neutral-100);
  --drift-border: rgba(62,43,184,0.22);
  --drift-indicator: rgba(62,43,184,0.55);

  /* State semantics (use annotations, not red) */
  --state-info: var(--c-cyan-500);
  --state-growth: var(--c-green-500);
  --state-attention: rgba(62,43,184,0.55);
  --state-critical: var(--c-neutral-950);

  /* Charts */
  --chart-grid: rgba(233,231,255,0.55);
  --chart-axis: rgba(11,10,20,0.52);

  --chart-1: var(--c-purple-900);
  --chart-2: var(--c-purple-700);
  --chart-3: var(--c-purple-500);
  --chart-4: rgba(124,103,255,0.55);
  --chart-5: var(--c-cyan-500);
  --chart-6: rgba(64,224,208,0.55);
  --chart-7: var(--c-green-500);
  --chart-8: rgba(47,230,166,0.55);
  --chart-9: var(--c-neutral-700);
  --chart-10: rgba(42,41,66,0.55);

  /* ============================================================
     Pillars (Base/Tint/Soft) — for brushing tags/rails/nodes
     ER SR SC CR II DM
     ============================================================ */
  /* Emotional Regulation (ER) */
  --pillar-er-base: var(--c-purple-900);
  --pillar-er-tint: var(--c-purple-200);
  --pillar-er-soft: var(--c-purple-100);

  /* Stress Resilience (SR) */
  --pillar-sr-base: var(--c-purple-700);
  --pillar-sr-tint: var(--c-purple-200);
  --pillar-sr-soft: var(--c-purple-100);

  /* Social Connectivity (SC) */
  --pillar-sc-base: var(--c-cyan-500);
  --pillar-sc-tint: var(--c-cyan-200);
  --pillar-sc-soft: var(--c-cyan-100);

  /* Cognitive Reframing (CR) — blue-teal between cyan/green */
  --pillar-cr-base: rgba(43,207,193,1);
  --pillar-cr-tint: var(--c-cyan-200);
  --pillar-cr-soft: var(--c-cyan-100);

  /* Identity Integration (II) */
  --pillar-ii-base: var(--c-purple-500);
  --pillar-ii-tint: var(--c-purple-200);
  --pillar-ii-soft: var(--c-purple-100);

  /* Decision Mastery (DM) */
  --pillar-dm-base: var(--c-green-500);
  --pillar-dm-tint: var(--c-green-200);
  --pillar-dm-soft: var(--c-green-100);

  /* ============================================================
     Component-level defaults (optional convenience variables)
     ============================================================ */

  /* Tag Chip (pillar-brushed) */
  --chip-surface: var(--surface-panel);
  --chip-ink: var(--ink-primary);
  --chip-border: var(--surface-border);
  --chip-dot: var(--c-purple-500);

  /* Eyebrows */
  --eyebrow-ink: var(--ink-secondary);
  --eyebrow-accent: var(--c-cyan-500);

  /* Portal Field (ambient) */
  --field-ambient: radial-gradient(1200px 800px at 20% 10%, rgba(124,103,255,0.18), rgba(0,0,0,0)),
                   radial-gradient(900px 700px at 80% 30%, rgba(64,224,208,0.12), rgba(0,0,0,0)),
                   radial-gradient(900px 700px at 60% 80%, rgba(47,230,166,0.10), rgba(0,0,0,0));
}

/* ---------- DARK MODE ---------- */
[data-theme="dark"] {
  --surface-bg: var(--c-neutral-950);
  --surface-panel: var(--c-neutral-900);
  --surface-card: var(--c-neutral-900);
  --surface-elevated: var(--c-neutral-900);
  --surface-border: rgba(233,231,255,0.14);
  --surface-divider: rgba(233,231,255,0.12);

  --ink-primary: var(--c-neutral-100);
  --ink-secondary: rgba(251,250,255,0.72);
  --ink-tertiary: rgba(251,250,255,0.52);
  --ink-inverse: var(--c-neutral-950);

  /* Trace + Handrail become “infrastructure dark” */
  --trace-surface: var(--c-neutral-900);
  --trace-border: rgba(233,231,255,0.14);
  --handrail-surface: var(--c-neutral-900);
  --handrail-border: rgba(233,231,255,0.14);

  /* Drift stays serious without warm hues */
  --drift-border: rgba(124,103,255,0.20);
  --drift-indicator: rgba(124,103,255,0.45);

  /* Elevation feels subtle on dark */
  --e1: 0 8px 24px -10px rgba(0,0,0,0.45);
  --e2: 0 16px 48px -18px rgba(0,0,0,0.55);

  /* Ambient field tuned for dark */
  --field-ambient: radial-gradient(1200px 800px at 20% 10%, rgba(124,103,255,0.14), rgba(0,0,0,0)),
                   radial-gradient(900px 700px at 80% 30%, rgba(64,224,208,0.10), rgba(0,0,0,0)),
                   radial-gradient(900px 700px at 60% 80%, rgba(47,230,166,0.08), rgba(0,0,0,0));
}

/* ---------- ATTENTION MODES (Calm / Focus) ---------- */
[data-attention="calm"] {
  --ui-density: 1;                 /* comfortable */
  --max-choices-per-scene: 3;
  --copy-length: 1;                /* short */
  --motion-multiplier: 1;
}

[data-attention="focus"] {
  --ui-density: 0.85;              /* compact */
  --max-choices-per-scene: 1;
  --copy-length: 0.75;             /* minimal */
  --motion-multiplier: 0.85;
}

/* ---------- ACCESSIBILITY: Reduce Motion ---------- */
[data-reduce-motion="true"] {
  --motion-multiplier: 0;
  --dur-arrive: 0ms;
  --dur-glide: 0ms;
  --dur-settle: 0ms;
  --dur-confirm: 0ms;
  --dur-carry: 0ms;
}

/* ---------- ACCESSIBILITY: High Contrast ---------- */
[data-contrast="high"] {
  --surface-border: rgba(233,231,255,0.85);
  --surface-divider: rgba(233,231,255,0.85);
  --ink-secondary: rgba(11,10,20,0.88);
  --focus-ring-subtle: rgba(64,224,208,0.55);
}

[data-theme="dark"][data-contrast="high"] {
  --surface-border: rgba(233,231,255,0.55);
  --surface-divider: rgba(233,231,255,0.55);
  --ink-secondary: rgba(251,250,255,0.88);
}

/* ============================================================
   Utility snippets (optional): pillar brushing helpers
   - Use these as patterns in CSS-in-JS or utility classes.
   ============================================================ */

/* Example: Pillar chip styling (set --p-base/--p-tint/--p-soft at runtime) */
.pillar-chip {
  background: var(--p-soft, var(--surface-panel));
  border: 1px solid var(--p-tint, var(--surface-border));
  color: var(--ink-primary);
  border-radius: var(--r-xl);
  padding: 6px 10px;
}
.pillar-chip::before {
  content: "";
  display: inline-block;
  width: 8px; height: 8px;
  border-radius: 999px;
  background: var(--p-base, var(--c-purple-500));
  margin-right: 8px;
  transform: translateY(-1px);
}

/* Example: Pillar rail (set --p-base) */
.pillar-rail {
  border-left: 3px solid var(--p-base, var(--c-purple-900));
  padding-left: var(--s-4);
}

/* Example: Thread line gradient */
.thread-gradient {
  background: linear-gradient(90deg, var(--thread-g0), var(--thread-g1), var(--thread-g2));
}
/* ============================================================
   Component-to-Color Matrix (CSS variable bindings)
   You can set these per-component for consistency at scale.
   ============================================================ */

/* LensControl */
.lens-control {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-active-underline: var(--focus-ring);
  --cmp-icon-accent: var(--focus-ring);
}

/* ReturnButton */
.return-button {
  --cmp-surface: var(--return-surface);
  --cmp-surface-hover: var(--return-surface-hover);
  --cmp-ink: var(--return-ink);
  --cmp-focus: var(--return-focus);
  --cmp-glow: var(--return-glow);
}

/* DepthDial */
.depth-dial {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-active: var(--c-neutral-200);
  --cmp-indicator: var(--focus-ring);
}

/* UniversalPlayer */
.universal-player {
  --cmp-surface: var(--surface-card);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-progress: linear-gradient(90deg, var(--thread-g0), var(--thread-g1), var(--thread-g2));
  --cmp-focus: var(--focus-ring);
}

/* PortalDock */
.portal-dock {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-accent: var(--focus-ring);
}

/* LoopRunner */
.loop-runner {
  --cmp-surface: var(--surface-card);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-ring: linear-gradient(90deg, var(--thread-g0), var(--thread-g1), var(--thread-g2));
  --cmp-step-active: var(--focus-ring);
  --cmp-seal: var(--trace-seal);
}

/* GripGenerator */
.grip-generator {
  --cmp-surface: var(--surface-card);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-focus: var(--focus-ring);

  /* intent accents */
  --intent-anchor: var(--c-purple-500);
  --intent-clarity: var(--c-cyan-500);
  --intent-connection: var(--c-cyan-500);
  --intent-direction: var(--c-green-500);
}

/* ReceiptForge */
.receipt-forge {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-seal: var(--trace-seal);
  --cmp-seal-soft: var(--trace-seal-soft);
}

/* TraceTile */
.trace-tile {
  --cmp-surface: var(--trace-surface);
  --cmp-border: var(--trace-border);
  --cmp-ink: var(--trace-ink);
  --cmp-seal: var(--trace-seal);
  --cmp-seal-soft: var(--trace-seal-soft);

  /* Optional pillar rail (set per instance) */
  --cmp-pillar-rail: var(--surface-border);
}

/* ThreadView */
.thread-view {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-line: var(--thread-line);
  --cmp-node: var(--thread-node);
  --cmp-active-node: var(--focus-ring);
  --cmp-cluster-glow: rgba(124,103,255,0.18);
}

/* TraceTravel */
.trace-travel {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-transition-glow: var(--return-glow);

  --view-me: var(--c-purple-500);
  --view-care: var(--c-cyan-500);
  --view-system: var(--c-neutral-700);
}

/* RoomSwitcher */
.room-switcher {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);

  --room-journeys: var(--c-purple-500);
  --room-navicues: var(--c-cyan-500);
  --room-toolkit: rgba(124,103,255,0.55);
  --room-wellbeing: var(--c-green-500);
  --room-state: var(--c-cyan-500);
  --room-trust: var(--c-neutral-700);
}

/* ConductorView */
.conductor-view {
  --cmp-surface: var(--surface-card);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-route-line: var(--focus-ring);
  --cmp-why-accent: var(--c-cyan-500);
  --cmp-governance-edge: rgba(233,231,255,0.55);
}

/* FeedWithSpine */
.feed-with-spine {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-spine-rail: var(--c-purple-900);
  --cmp-spine-node: var(--c-cyan-500);
}

/* SpineAtlas */
.spine-atlas {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-grid: var(--chart-grid);
  --cmp-node: var(--c-neutral-700);
  --cmp-node-active: var(--focus-ring);
  --cmp-depth-overlay: rgba(62,43,184,0.08);
}

/* NaviCueGallery */
.navicue-gallery {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-card: var(--surface-card);
  --cmp-accent: var(--c-cyan-500);
  --cmp-proof-chip: var(--trace-seal);
}

/* JourneyStudio */
.journey-studio {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-week-glow: rgba(124,103,255,0.18);
  --cmp-receipt-seal: var(--trace-seal);
  --cmp-anchor: var(--c-purple-900);
}

/* ConsentMap */
.consent-map {
  --cmp-surface: var(--handrail-surface);
  --cmp-border: var(--handrail-border);
  --cmp-ink: var(--handrail-ink);
  --cmp-edge: var(--handrail-edge);
  --cmp-toggle-on: var(--c-cyan-500);
  --cmp-toggle-off: rgba(42,41,66,0.35);
}

/* EscalationRail */
.escalation-rail {
  --cmp-surface: var(--handrail-surface);
  --cmp-border: var(--handrail-border);
  --cmp-ink: var(--handrail-ink);
  --cmp-rail: var(--handrail-edge);

  /* Serious state without warm danger hues */
  --cmp-serious-surface: var(--c-neutral-950);
  --cmp-serious-ink: var(--c-neutral-100);
  --cmp-serious-edge: var(--c-cyan-500);
}

/* IntegrityLogPreview */
.integrity-log {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);
  --cmp-mono: var(--ink-primary);
  --cmp-accent: var(--c-cyan-500);
  --cmp-row-alt: rgba(233,231,255,0.30);
}

/* GovernanceLockMap */
.governance-lock-map {
  --cmp-surface: var(--surface-panel);
  --cmp-border: var(--surface-border);
  --cmp-ink: var(--ink-primary);

  --ring-locked: var(--c-purple-900);
  --ring-controlled: var(--c-purple-500);
  --ring-expandable: var(--c-cyan-500);

  --cmp-label: var(--ink-secondary);
}

/* IntegrationsOverlay */
.integrations-overlay {
  --cmp-surface: var(--surface-bg);
  --cmp-ink: var(--ink-primary);
  --cmp-signal-in: var(--c-cyan-500);
  --cmp-missed-out: var(--c-green-500);
  --cmp-privacy-edge: rgba(233,231,255,0.55);
}

```
